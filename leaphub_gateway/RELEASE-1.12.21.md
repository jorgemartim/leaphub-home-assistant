# Gateway 1.12.21

As respostas privadas de saúde agora informam `Connection: close` e encerram o socket imediatamente após o corpo JSON. O healthcheck público preserva keepalive. Comandos aceitos pela nuvem permanecem explicitamente como confirmação pendente até uma leitura nova do veículo, sem reenvio físico.
