name: Validate repository

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - "repository.yaml"
      - "leaphub_gateway/**"
      - ".github/scripts/**"
      - ".github/workflows/validate.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install validator and runtime test dependencies
        run: python -m pip install --disable-pip-version-check -r leaphub_gateway/requirements.txt PyYAML==6.0.2 "pytest>=8,<10"

      - name: Validate repository and Python files
        run: python .github/scripts/validate_repository.py
