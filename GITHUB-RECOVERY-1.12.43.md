# Recuperação GitHub — Gateway 1.12.43

O release mantém a publicação em duas fases: primeiro a imagem GHCR 1.12.43, depois a promoção de `config.yaml`.

A atualização não deve remover `/data`, `ocpp-state.sqlite`, filas ou opções do Home Assistant.
